<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.personallibraryv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LendInfoView"></activity>
        <activity android:name=".LendInfoAdd" />
        <activity android:name=".BookView" />
        <activity android:name=".Statistics" />
        <activity android:name=".LendingInfo" />
        <activity android:name=".WishList" />
        <activity android:name=".ReadingList" />
        <activity android:name=".addNewBook" />
        <activity android:name=".Collection" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>